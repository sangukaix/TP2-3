# 후보 지역 적합성·예산·측정 비교 (2026-09-08)

사용자가 남은 지역 적합성·예산·성과 측정·전체 연결·반복성 검증을 재개하도록 요청했다. 먼저 동일 원주 동결 근거로 후보 두 개의 적합성·이용 절차·구조화 비용·측정을 작성하는 독립 진단을 실행한다. 후보 내용이 미달이면 이를 Gemma에 전달해 승인 가능한 전체 기획으로 취급하지 않는다.

## 경계와 재현

- 운영 Agent·프롬프트·호출 상한·ML·저장 보고서·승인 상태는 변경하지 않는다.
- 진단은 OllamaProvider만 직접 호출한다. OpenAI/Router/보고서 저장/웹/추가 도구 조회는 호출하지 않는다. 요청별 로컬 최종 호출 1회, 자동 교정 없음.
- D-107 동결 입력 SHA `9271601f46105352ab0d06e64866ba4c0d2888568cc248aeeeb188b366846b92`를 검증하고 기존 중간 근거 메시지를 그대로 보존한다. 시스템 지시·최종 과제·Schema만 이 진단에 맞춰 변경한다.
- 모델 Qwen3:14b, think=False, temperature0.15, 문맥40960, 출력5600, timeout420초. 예산 미정인 기존 조건을 유지하고 가정 견적/null을 허용한다.
- Python Decimal로 항목 곱셈/소계 합을 확인한다. 이는 예산 운영량·시장 단가·항목 완전성·확정 여부·지역 타당성 검증이 아니다. `report_approved=false`, `semantic_approval=false` 유지.
- 첫 제한 환경 연결 실패 기록: `storage/candidate_feasibility_0f50b3a0d55d4dbaac3ca6b31b8a5426.json`, 267ms, 사용량 미보고. 승인된 네트워크 실행은 별도 파일로 보존한다.

```powershell
backend/.venv/Scripts/python.exe -m ai_server.app.scripts.evaluate_candidate_feasibility storage/candidate_comparison_input_5207b2af04f74b3d909b4cd2abf7dd14.json --run
# 비교군: 위 명령에 아래 인자 추가
# --reviewed-example docs/examples/wonju_candidate_flow_draft.json
backend/.venv/Scripts/python.exe -m unittest ai_server.test_candidate_feasibility ai_server.test_candidate_flow ai_server.test_candidate_replay ai_server.test_candidate_thinking_comparison -q
```

`--run` 없이는 준비 파일만 저장한다. 예시 사용은 기존 명시적 사용자 승인 예시에 한정하며, 기존 함수가 미검토 예시와 변조된 동결 입력을 거절한다. 신규 예시를 사람 검토 완료로 표시하지 않는다.

## 예시 없는 실제 응답: 품질 미달

증빙: `storage/candidate_feasibility_60a3576ce6f4493fa6a1ae23fe576486.json`. 요청 SHA `f4487545e76e1d16c670715b7a31ad662ed554ef519b844c9d4c6024b9273264`, 111.049초, 입력15,672/출력1,749토큰, 완전 JSON/Schema 통과.

1. **지역 적합성 실패:** 후보 제목이 `전주시 2026년도 관광산업과 세출예산 사업명세서`, `강진 반값여행 지역경제 효과 소개`다. local_facts는 원주 관측이 아니라 해당 사례 자체다. 지역 필요성을 검토한 두 신규 사업 후보가 아니다.
2. **이용 절차 실패:** 첫 후보 actor가 사무관리·행사운영·민간경상보조라는 예산 항목이며 두 번째는 지역상품권을 환급 담당자로 적는다. 실제 담당 역할·신청 자격 확인·취소/중복 증빙 절차가 없다.
3. **예산 의미 오류:** 전주안 operating_volume은 총10억원인데 네 항목을 각각1건×1억원, 총4억원으로 작성한다. 곱셈과 합계 검사에는 문제가 없지만 운영량/총액이 모순이다. 제공 카드의 총예산을 세부 단가의 증거로 삼아 confirmed로 표시한 것도 부적절하다. 강진안은 null/unknown을 유지했으나 운영량·환급 상한·인건비 등이 설계되지 않았다. null은 허위 숫자보다 정직하지만 완성 견적은 아니다.
4. **측정 실패:** 야간 이용자/전체 방문자를 분자·분모로 쓰면서 수집 원장은 예산서, 담당은 사무관리다. 강진안 역시 지역상품권을 수집 담당으로 적고 소개 자료를 원장으로 취급한다. 사업 참여자와 지역 전체 모집단의 정합성·비교 설계가 없다.
5. **부정 판단의 한계:** 두 후보 reject와 성공 기준 미정은 유지했다. 그러나 사업을 제대로 설계하지 못한 것과 근거에 따라 타당하게 기각한 것은 다르다. 올바른 거절 능력을 입증한 결과로 해석하지 않는다.

## 다섯 검증 항목의 진행 기준

| 항목 | 이번 확인 / 다음 통과 조건 |
|---|---|
| 지역 적합성 | 관측 사실별 원주 출처·기간·단위와 필요성 가설 분리. 비교/수요 근거 없으면 보류. 현재 실패 |
| 예산 | 곱셈/합계 외 운영량·상한·누락비·확정 근거 검토. 현재 산식 통과 항목도 의미 실패 |
| 성과 측정 | 분자/분모의 모집단·중복/취소·원장 확보·수집자·기간·비교가능성. 현재 실패 |
| 전체 연결 | 검토 가능한 후보가 확보된 후 후보→Gemma→로컬 검토를 독립 진단에서 대조. 현재 선행 후보 미달로 미실행. 유료 최종 검수는 별도 제한 유지 |
| 반복성 | 동일 조건 반복과 별도 지역의 실제 근거 입력을 분리. 지역명만 치환하거나 예시와 평가 입력을 섞지 않음. 현재 원주 단회 비교로 미검증 |

이전 후보 하나 실험과 이번 두 후보·예산·측정 과제는 다르므로 시간/오류 수의 직접 성능 비교를 하지 않는다. 기존 승인 예시 유무 비교도 각1회여서 일반화·인과적 품질 개선을 주장할 수 없다.

## 승인 예시 포함 응답과 비교: 여전히 미달

증빙: `storage/candidate_feasibility_a2c432106e2c43e5880beb3dd1078416.json`, 요청 SHA `21ddb48746cad4e1a305c26c077fa741ac4926d7f5b778365cf98aaf31d9b782`. 예시 메시지 하나를 제거하면 두 요청의 모델·messages·Schema·출력 상한이 동일함을 실제 파일끼리 대조했다. 원주 작성 예시는 기존 사용 승인본이며 추가 사실 원문을 수집하지 않았다.

| 각1회 | 예시 없음 | 승인 예시 포함 |
|---|---:|---:|
| 시간 | 111.049초 | 154.084초 |
| 입력 토큰 | 15,672 | 16,615 |
| 출력 토큰 | 1,749 | 2,768 |
| 완전 JSON/Schema | 통과 | 통과 |
| 보고서 승인 | false | false |

- **변화:** 원주 2026-06 소비60,562,672,000원(dataset3), 숙박비율12.5%(dataset4)를 올바르게 연결했다. 필요성은 hypothesis 필드로 분리했다. 임의 확정 금액 대신 null로 처리했다.
- **지역 필요성:** 그 두 관측값으로 환급/야간사업 중 어느 것이 우선인지 판단할 근거는 여전히 없다. 필요한 지역 수요·업체 참여 가능성 대신 이미 사업이 증가 효과를 낸 직접 데이터를 요구한다. 신규 사업 검토 전에 보장 효과가 있어야 한다는 식의 기각 사유도 부적절하다.
- **이용 절차:** 환급안은 신청→결제 증빙→중복/취소 확인→지급/사용 대조로 구체화됐지만 제목/단계가 제공 예시와 거의 같아 독립 설계나 일반화의 증거가 아니다. 야간안은 `체류 시간과 소비 증가를 경험한다`는 표현을 사용자의 행동으로 작성한다. 서비스/신청/결제 절차는 여전히 없다.
- **사례 해석:** 야간 후보 제목에 예산 편성이 남고 전주 예산 구조를 운영 원리로 인용한다. 확정 효과를 부정하는 문구만으로 실제 콘텐츠 설계가 보완되지는 않는다.
- **비용:** 두 후보 수량·단가·소계·총액을 모두 null로 남겼다. 미확정 숫자를 만들지 않은 것은 바람직하지만 참고 견적과 운영량을 검증할 수 없다. `assumption` 대신 실질적으로 unknown인 항목도 있다. exclusions는 누락 비용 대신 효과 자료 부족/위험을 복사한다. 비용 완전성 미달이다.
- **측정:** 야간안은 시간과 소비 증가를 하나의 분자로 섞고, 환급안은 `지급에 따른 후속 결제 증가 / 지급 전 후속 결제`라고만 적는다. 금액/건수/인원 단위, 취소·중복 처리, 동일 참여 집단·동일 기간·0분모 처리, 실제 원장 항목/확보 여부가 없다. `증가 집계`는 수집 원장이 아니다. 기간2026-07~12도 입력 일정이 미정인 상태에서 제안 표기/근거 없이 선택했다.
- **결론:** 예시 모방과 일부 출처 연결은 관찰했지만 지역 적합성·실행 설계·예산·측정 모두 통과하지 못했다. Gemma 본문과 최종 검수는 실행하지 않았다. 다른 지역 검증/반복 실험도 아직 수행하지 않았다. 성공률이나 일반적인 품질 개선을 주장하지 않는다.

## 검증 결과와 다음 분리 과제

관련 unittest **12개 통과**. 동결 근거/상한 보존·hash 변조 거절·예시만 변하는 대조·미검토 예시 거절·Decimal 계산·null을0원으로 취급하지 않음을 확인했다. git diff 공백 검사 통과(기존 LF/CRLF 안내 있음). pytest는 기존 환경에 미설치이고 새 설치를 하지 않았다. 운영/UI/ML 변경이 없어 frontend build·학습·유료 최종검수를 실행하지 않았다.

다음에는 같은 실패 입력으로 운영 호출을 확대하지 말고 **가정 운영량을 명시한 별도 평가 입력**에서 비용과 측정 설계를 분리해 확인한다. 이 입력의 수량/단가는 공식 사실이 아니라 평가용 조건으로 표시해야 한다. 기존 원주 예시와 다른 과제를 사용해 모방 효과를 구분하고, 비용 항목·참여자 원장·비교 지표를 먼저 검토한 뒤 후보/본문 연결을 판단한다. 실제 원주 사업비 확정·운영 분할 호출 추가·파인튜닝 결정은 아니다.

## 사용자 방향 조정: 필요성 증명보다 사례 기반 발전 가능성

사용자는 관측값으로 사업 필요성을 증명하는 것보다, 유사한 타지역 사례를 비교해 원주에서 무엇을 더 기획할 수 있고 어느 정도까지 발전 가능성을 시험할지를 우선하도록 했다. 예산은 정확한 견적이 아니어도 가정 수량·단가가 드러나는 임시 추정치를 허용했다. 이에 따라 발전 가능성은 방문·매출 증가 보장이 아니라 참여업체·이용완료·운영일·재사용액 등 시범사업이 직접 관리할 수 있는 목표로 정의했다.

### 발전 방향 두 개 통합 요청

`storage/candidate_development_539f449bed3d43878af6cf67bed8ad08.json`. 327.625초, 입력15,765/출력5,600토큰 후 `OLLAMA_INCOMPLETE_RESPONSE`. 자동 재시도나 잘린 응답 결합을 하지 않았다. 사례 비교 두 방향·각 이용 흐름·예산·측정을 한 호출에 요구하는 방식은 이번 입력/상한에서 완료되지 않았다.

### 시범 방향 하나로 축소한 첫 응답

`storage/candidate_pilot_301f4f342db847c59006bb4b7937a254.json`. 73.506초, 입력15,727/출력1,261토큰. 응답 JSON은 축소 Schema와 4천만원 산식을 사후 대조해 통과했으나 당시 진단 코드가 이전 Schema로 검사하여 `ValidationError`로 잘못 기록했다. 코드를 수정한 뒤 같은 응답을 다시 생성하지 않았다.

- 야간 콘텐츠10곳·참여업체50곳·이용완료100건이라는 관리 가능 목표를 제시했다.
- 현재 기준을 근거 없이0으로 만들고 확인되지 않은 `시 관광진흥원`을 담당자로 지정했다.
- data_record에 수집 원장 대신 기관명을 적고, 현재 2026-09보다 과거인 2025-07~2026-06을 기록처럼 넣었다.
- 4천만원 합계는 맞지만 단가 근거와 누락 비용이 모호해 내용 품질은 미달이다.

### 시점·역할·원장 조건을 명확히 한 축소 응답

`storage/candidate_pilot_0bb4967cecc14d89933aafc44f2e38e9.json`. 77.196초, 입력15,972/출력1,310토큰. 완전 JSON이며 축소 Schema를 사후 대조했다. 후처리에서 `assumed_unit_price_krw`를 이전 필드명으로 읽은 진단 버그 때문에 프로세스가 종료됐고, 보존된 응답을 오프라인으로 재검증했다. GPU 재호출은 하지 않았다.

- **사례 비교:** 전국 야간관광 특화도시 운영과 야간축제 사례를 골라 상설/행사 콘텐츠와 식음·교통·안전 조건을 비교하려는 방향은 적절하다. 다만 `성과 수치는 확인되지 않았다`는 표현은 특화도시 카드의 관측3%를 뭉뚱그리며, 그 수치를 원주에 복사하지 않은 점은 유지했다.
- **원주 적용:** 야간 콘텐츠2개·참여업체5개·운영10일의 작은 시범으로 좁혔다. 그러나 근거 pack에서 확인되지 않은 뮤지엄산 야간 프로그램·오크밸리 야간 이벤트를 예시로 제시했다. 운영 가능성이 확인된 장소로 취급할 수 없다.
- **이용 절차:** 담당 역할의 기획→업체 신청→선정/협약→준비까지만 있고 관광객 예약·이용/결제·완료 확인이 없다. 사업 준비 흐름이지 실제 이용 흐름은 아니다.
- **임시 예산:** 운영2개×100만원200만원, 업체5개×50만원250만원, 기타1건×100만원100만원, 총550만원. 산식은 맞고 임시 추정 표시도 있다. 그러나 운영10일이 비용에 연결되지 않고 `기타 비용`은 구체 항목이 아니며 홍보·안전·교통·보험·측정 등 제외 범위가 충분하지 않다. 확정 견적으로 사용할 수 없다.
- **발전 가능성:** 협약업체5개와 이용완료100건을 관리 목표로 둔 것은 근거 없는 지역 전체 증가율보다 낫다. 기준은 `미확인-시범 전 조사`로 고쳤다. 이용완료 계산의 `평균 이용자 수` 값이 없고, 이용 원장의 필드·취소/중복 처리·업체별 합산 방식은 부족하다.
- **종합:** 사용자 요청 방향에는 가까워졌지만 **부분 통과**다. 유사 사례 비교→시범 규모→임시 예산→운영 목표의 뼈대는 확인했다. 장소 검증, 관광객 이용 흐름, 비용 항목, 측정 원장이 남아 Gemma 전체 본문이나 운영 프롬프트에 반영하지 않았다.

## 구조화 시범 계약과 전체 로컬 연결

사용자 방향에 맞춰 사례 비교·원주 적용 설명은 LLM이 맡고, 운영량·임시 예산·측정 계약은 서버 입력으로 보존하는 실험을 추가했다.

### 서버 소유 평가 계약

- 범위: 야간 콘텐츠2개, 운영10일, 참여업체5개, 유효 이용완료 목표100건.
- 임시 추정: 콘텐츠20콘텐츠·일×50만원=1,000만원, 안전·이동10일×30만원=300만원, 업체 준비5개×50만원=250만원, 예약·결제·측정 원장1식×200만원=200만원, 총 **1,750만원**.
- 제외: 시설 임차/신규 설비, 대규모 광고, 교통기관 할인 협약 비용.
- 측정1: 유효 이용완료율=`취소·중복 제외 이용완료ID/승인 예약ID×100`.
- 측정2: 유효 참여자의 야간 연계 결제액=`유효 완료ID에 연결된 허용업체 취소 제외 결제액 합계`.
- 위 값은 실제 원주 예산·성과 예측이 아니라 분할 생성의 일관성을 검증하는 **평가용 기획 가정**이다.

첫 계약 출력 `storage/candidate_pilot_contract_5cf290617e04449d9a24725fe0df5002.json`은 46.871초, 입력1,879/출력1,196토큰이었다. 산식/운영 흐름은 맞았으나 모델이 100건·구체 원장을 일반적인 방문/소비 증가율로 바꿨다. 측정 필드를 Schema에 명시한 재실행 `storage/candidate_pilot_contract_f4eccc5ba9d146078590faf764d6889b.json`은 49.171초, 입력1,879/출력1,462토큰이다. 완전 JSON, 예산 산식 및 정적 검사 통과이나 입력의 측정 목표를 운영역량/규모확장이라는 추상 문구로 다시 썼다.

결론적으로 LLM이 숫자 계약을 재작성하지 않게 했다. `apply_pilot_contract`가 마지막 LLM 서술에 서버 계약을 오프라인 결합한 `storage/candidate_pilot_composed_400d234574eb446794ed552a61eb2249.json`을 만들었다. 추가 LLM 호출 없이 Schema·1,750만원 산식·유효이용100건·분자/분모·취소/중복·결제ID 정적 검사를 통과했다. 의미/사업 승인 false는 유지한다.

### Gemma 본문 연결

`storage/candidate_pilot_planner_6917d3fa759f47f09efc378c7f1db45b.json`. 제한 환경 연결 실패 1건은 별도 보존했고, 승인된 로컬 실행은 31.137초, 입력2,424/출력762토큰이다. Schema와 두 사례 ID가 일치했고 서버 소유 예산/측정 계약을 별도로 보존했다.

Gemma는 야간관광 특화도시/야간축제의 콘텐츠·식음·교통·안전·상점 연결을 원주10일 시범의 설계→모집/예약→운영/완료→취소·중복 제거 평가로 연결했다. 타지역3%를 원주 효과로 복사하지 않았고 이용완료100건을 관리 목표로 구분했다. 그러나 본문 예산 설명에서 총1,750만원을 누락하고, 원장 설계 단계에서 `예약·결제 시스템 구축`, `실시간 이용 현황`을 사용해 현재 확정 범위를 확대했다.

### Qwen 검토와 결정적 재검수

`storage/candidate_pilot_reviewer_9d932c891fae4bd08a264cad477c1f0f.json`. Qwen은 14.511초, 입력1,988/출력232토큰에 blocking issue 없이 승인했다. 사례/임시예산/100건/효과 비보장 체크를 모두 true로 반환했지만 위 세 본문 오류를 잡지 못했고 다음 행동에 시스템 구축을 다시 권했다. 따라서 이 LLM 승인은 채택하지 않는다.

`storage/candidate_pilot_final_audit_5b4a80ec29674c4486bf46806aae4779.json`에서 서버가 `본문의 임시 총액 누락`, `근거 없는 실시간 운영`, `근거 없는 시스템 구축`을 확인해 최종 `report_approved=false`로 재판정했다. **전체 로컬 연결은 기술적으로 완료됐지만 의미 품질은 미승인**이다.

현재 결론은 다음과 같다.

1. 유사 사례 비교→원주 시범→Gemma 본문→Qwen 검토까지 짧은 분할 연결은 동작한다.
2. 구체 계약을 별도 보존하면 예산·측정 숫자 손실을 막을 수 있다.
3. LLM Reviewer도 과장을 놓치므로 결정적 검사가 최종 승인에 필요하다.
4. 이 구조는 독립 진단에만 있고 운영 Agent에는 아직 반영하지 않았다.
5. 다른 지역/독립 입력 반복성은 여전히 미검증이다.

관련 독립 진단 unittest **25개 통과**. 입력/계약 보존, 사례 제한, Schema, 산식, 측정 필드, Gemma 전달, Reviewer 결정 검사를 확인했다. FastAPI `on_event` deprecation 경고는 기존 코드에서 발생했다. 신규 라이브러리·OpenAI·웹 검색·DB/보고서 저장·ML 변경은 없었다.

## 2026-09-09 별도 지역 반복 입력: 강남구

### 독립 입력 확보

저장 강남 보고서 `81a10a4c42b84fd89410acb73e984fec`에서 이전 후보를 제외한 `--fresh --capture-only` 준비를 실행했다. 도구 선택 3회만 수행하고 최종 후보 LLM 호출은 차단했다. 결과 `storage/candidate_comparison_input_b750ae3144f644e9a7879e88491e9c03.json`의 공통 요청 SHA는 `44e08dcb5a15d36328e0fdad1fb2df3a09570d4f9cab22239d9f9039d20d1600`이며 지역은 서울특별시 강남구(11680)다. 원주 동결 SHA `9271601f46105352ab0d06e64866ba4c0d2888568cc248aeeeb188b366846b92`와 다른 독립 입력이다.

초기 강남 계약 실행 `storage/candidate_pilot_contract_3d082de51a704256933deb724ead2dad.json`과 재시도 `storage/candidate_pilot_contract_e8b152e96bb048929955bc0b53b9f061.json`은 각각 21.350초·21.153초 뒤 `OLLAMA_CONNECTION_ERROR`로 끝났다. 응답·사용량이 없으므로 품질 실패 건수로 계산하지 않는다.

### 기존 실제 강남 보고서의 내용 대조

MySQL 원문을 읽기 전용으로 확인했다. 이 보고서는 현재 student_budget 실험이 아니라 hybrid 실행 결과이며, Qwen transferability와 Gemma planner가 모두 `OLLAMA_CONTEXT_BUDGET_EXCEEDED`로 실패한 뒤 OpenAI fallback이 작성했다. 최종 검수는 74점, `approved=false`다.

- 강남의 숙박 방문 비율3.1%, 식음·쇼핑 소비 비중, 인천 올 나이츠 사례와 야간관광 특화도시 사례를 연결한 방향 자체는 확인된다.
- 그러나 최종 비교문은 인천 사례를 참고한다고만 축약해 야시장·야간마켓·공연·상권쿠폰·시티투어·바 호핑 중 무엇을 가져오고, 강남의 한 권역 패스에서 무엇을 축소·제외하는지 충분히 보존하지 못했다.
- KPI는 운영일의 이용완료율·당일결제율을 비운영일과 비교한다고 쓰지만 비운영일에는 예약·입장 분모가 성립하지 않는다. 분자·분모와 비교 집단이 같은 모집단인지 명확하지 않다.
- 따라서 이 기존 보고서도 실제 응답 내용 기준으로 품질 미달이며 새 강남 실험의 정답이나 승인 예시로 사용하지 않는다. 다만 실패 조건과 비교 사례 선택을 고정하는 기준선으로만 사용한다.

### 이름 치환을 막은 새 축소 요청

초기 지역 일반화 코드는 출력 필드명만 바꾸고 LLM 입력에는 강남 관측·관광자원 출처를 보내지 않았으며, 결정적 측정 한계에도 `원주시 전체`가 남아 있었다. 이를 다음과 같이 보완했다.

1. `regional_context`에 선택 지역의 등록 dataset 요약/source ID, 데이터랩 인기장소, 관광공사 Open API 관광자원을 작게 전달한다. 강남 입력은 dataset 7개와 인기장소·관광자원 9개다.
2. 관측은 사업 필요성 확정이 아니라 적용 방향과 시범에서 추가로 확인할 범위에만 사용한다. 장소 출처는 등록명·주소·순위만 뒷받침하며 운영시간·협약·야간 프로그램은 확인된 사실로 만들지 않는다.
3. 저장 공식 사례 요약 중 지역 방향과 맞는 야간 사례가 있으면 공통 야간관광 특화도시 카드와 짝지어 비교한다. 강남은 `case:night_tourism_cities_2024`와 `case:2c54c6aaf2117a`(올 나이츠 인천)다. 요약에 없는 세부 절차·성과 산식은 추가하지 않는다.
4. 출력 `case_application`에서 각 사례의 적용 장치, 지역 변경, 제외/미확인, 시범 학습 범위를 분리한다. `regional_evidence_source_ids`에는 요청에 있던 dataset ID 하나 이상과 장소/관광자원 ID 하나 이상을 요구하며, 응답 후 정확한 허용 목록과 대조한다.
5. 서버 계약 결합의 지역 전체 해석 한계도 계약의 실제 `region_name`을 사용한다. 1,750만원은 계속 평가용 임시 추정이며 강남 확정 예산이 아니다.

모델 미호출 준비 결과는 `storage/candidate_pilot_contract_9db7528779e0446392435fcf2659766b.json`이다. 요청 SHA `efe4b7087479db3cf32921dd4454d3bb35af7caf4853a911cdf413d8cc61e72b`, 부모 SHA `44e08dcb5a15d36328e0fdad1fb2df3a09570d4f9cab22239d9f9039d20d1600`이다. 지역명은 강남구이고 시스템 지시에 원주 문자열이 없으며, 두 사례·지역 출처·임시 총액을 모두 포함한다.

승인된 네트워크에서 설정된 Ollama `/api/tags`를 10초 읽기 전용으로 확인했지만 타임아웃이었다. 이 PC의 `127.0.0.1:11434`도 연결 거부이고 Ollama 실행 파일이 없어 같은 모델의 로컬 대체 실행은 불가능했다. 따라서 새 Qwen 응답의 실제 사례 해석·지역 적용·운영 흐름은 아직 보지 못했다. 연결 복구 뒤 위 준비 요청을 한 번 실행해 기존 74점 보고서의 두 핵심 실패가 실제 본문에서 사라졌는지 확인하기 전에는 반복성이나 품질 개선을 주장하지 않는다.

재실행 명령은 다음 한 번으로 제한한다.

```powershell
backend/.venv/Scripts/python.exe -m ai_server.app.scripts.evaluate_candidate_feasibility storage/candidate_comparison_input_b750ae3144f644e9a7879e88491e9c03.json --contract-pilot --run
```

지역 입력·사례 적용·출처 ID·서버 계약과 기존 후보 회귀를 합친 unittest **129개 통과**. Python 컴파일과 변경 파일 diff 공백 검사도 통과했다. 경고는 기존 FastAPI `on_event` 및 NumPy/Joblib deprecation뿐이다. pytest·새 라이브러리·유료 호출·보고서 저장·ML 재학습은 실행하지 않았다.
