# TP2-3 Python 서버 공통 의존성
# 설치: python -m pip install -r requirements.txt
# React/Vite 의존성은 frontend/package.json에서 npm install로 설치합니다.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
# AI 전략기획서 Word(.docx)와 원자료 추세 그래프 생성에 사용합니다.
python-docx>=1.2,<2.0
# bid3의 제안서 제작 장점을 관광 기획안에 적용하는 PowerPoint(.pptx) 출력입니다.
python-pptx>=1.0,<2.0
matplotlib>=3.10,<4.0
# 공식 정책·관광 문서를 지역 필터로 검색하는 영속형 RAG 저장소입니다.
chromadb>=1.0,<2.0
